<% content_for :page_title do %>
  <%= link_to Spree.t(:pages), spree.admin_pages_url %> /
  <%= Spree.t(:new_page) %>
<% end %>

<%= form_for [:admin, @page] do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>

